# Input tuple
input_tuple = ("python", "learn", "includehelp")

# Extract the last character from each string in the tuple
output = [word[-1] for word in input_tuple]

# Print the output
print(output)
